import LinearGradrient from 'react-native-linear-gradient';
import styled from 'styled-components/native';

export default styled(LinearGradrient).attrs({
  colors: ['#22202C', '#402845'],
})`
  flex: 1;
`;
